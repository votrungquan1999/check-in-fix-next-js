import { CSSProperties } from 'styled-components';

export const loginFormStyles: CSSProperties = {
  minWidth: 400,
  marginTop: 50,
};

export const loginFormButtonStyles: CSSProperties = {
  width: '100%',
};

export const loginPageStyles: CSSProperties = {
  width: '100vw',
  height: '100vh',

  alignItems: 'center',
  justifyContent: 'center',

  display: 'flex',
  flexDirection: 'column',
};
